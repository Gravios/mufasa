"""Mufasa workbench pages — one module per sidebar entry.

Each page module exposes a ``build_<stage>_page(workbench, config_path)``
function that attaches the page and its sections to the workbench.
"""
